# secrets/secrets.yaml — SOPS-ENCRYPTED secret store (PLACEHOLDER).
#
# !!! THIS IS A PLACEHOLDER — DO NOT USE FOR A REAL PASSWORD !!!
#
# The real file must be encrypted with `sops secrets/secrets.yaml` after
# authoring `secrets/.sops.yaml` (see README.md). Until then this file exists
# only so Nix can evaluate the `sops.defaultSopsFile` path. `nixos-rebuild
# switch` will FAIL while this placeholder is in place — that is intentional
# and forces the one-time bootstrap before the first switch.
users/sid/password: PLACEHOLDER_REPLACE_WITH_mkpasswd_HASH_BEFORE_FIRST_SWITCH
